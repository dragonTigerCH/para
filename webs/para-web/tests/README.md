# Playwright E2E 테스트 가이드

## 📋 목차

- [테스트 실행 방법](#테스트-실행-방법)
- [테스트 구조](#테스트-구조)
- [작성된 테스트](#작성된-테스트)
- [디버깅 팁](#디버깅-팁)

## 🚀 테스트 실행 방법

### 1. 기본 테스트 실행 (헤드리스 모드)

개발 서버가 자동으로 시작되고 테스트가 실행됩니다:

```bash
npm test
```

### 2. UI 모드로 테스트 (추천)

브라우저를 보면서 테스트를 실행하고 디버깅할 수 있습니다:

```bash
npm run test:ui
```

**장점:**
- 각 테스트 단계를 시각적으로 확인
- 타임라인으로 액션 추적
- 실패한 지점에서 멈춤
- DOM 상태 확인 가능

### 3. Headed 모드 (브라우저 보기)

브라우저 창을 열고 테스트를 실행합니다:

```bash
npm run test:headed
```

### 4. 특정 테스트 파일만 실행

```bash
npx playwright test tests/e2e/home.spec.ts
npx playwright test tests/e2e/navigation.spec.ts
```

### 5. 테스트 리포트 보기

테스트 실행 후 HTML 리포트를 볼 수 있습니다:

```bash
npm run test:report
```

## 📁 테스트 구조

```
tests/
└── e2e/
    ├── home.spec.ts         # 홈페이지 렌더링 테스트
    └── navigation.spec.ts   # 페이지 네비게이션 테스트
```

## ✅ 작성된 테스트

### home.spec.ts - 홈페이지 기본 렌더링

1. **페이지가 정상적으로 로드되어야 함**
   - 헤더 타이틀 확인
   - 아이콘 확인

2. **전체 보기 제목과 설명이 표시되어야 함**
   - "전체 보기" 제목
   - 설명 텍스트

3. **Pinned 카테고리 버튼이 표시되어야 함**
   - 버튼 존재 확인
   - 설명 텍스트 확인

4. **Inbox 카테고리 버튼이 표시되어야 함**
   - 버튼 존재 확인
   - 설명 텍스트 확인

5. **모든 PARA 카테고리가 표시되어야 함**
   - Project, Area, Resource, Archive 확인

6. **카테고리별 항목 개수가 표시되어야 함**
   - Pinned, Inbox 개수 표시

### navigation.spec.ts - 페이지 네비게이션

1. **Pinned 카테고리를 클릭하면 상세 페이지로 이동**
   - URL 변경 확인
   - 페이지 제목 확인

2. **Inbox 카테고리를 클릭하면 상세 페이지로 이동**
   - URL 변경 확인
   - 페이지 제목 확인

3. **Project 카테고리 상세 보기 클릭**
   - 상세 페이지 이동

4. **Back 버튼으로 홈 복귀**
   - 홈페이지로 돌아가기

5. **테이블 아이템 클릭**
   - 아이템 상세 페이지 이동

6. **아이템 페이지에서 Back 버튼**
   - 이전 페이지 복귀

7. **카테고리 섹션 토글**
   - 접기/펼치기 동작

## 🔍 디버깅 팁

### 1. UI 모드 사용 (가장 추천)

```bash
npm run test:ui
```

- 각 단계를 천천히 볼 수 있음
- 실패 시점에 멈춤
- DOM 검사 가능

### 2. 특정 테스트만 실행

테스트 파일에서 `test.only()`를 사용:

```typescript
test.only('이 테스트만 실행', async ({ page }) => {
  // ...
});
```

### 3. 스크린샷 확인

테스트 실패 시 자동으로 스크린샷이 저장됩니다:
```
test-results/
└── [테스트명]/
    └── test-failed-1.png
```

### 4. Trace 확인

실패한 테스트의 trace를 볼 수 있습니다:

```bash
npx playwright show-trace test-results/[테스트명]/trace.zip
```

### 5. Headed 모드로 직접 보기

```bash
npm run test:headed
```

브라우저가 열려서 실제 동작을 볼 수 있습니다.

## 📝 테스트 작성 예시

### 기본 구조

```typescript
import { test, expect } from '@playwright/test';

test.describe('테스트 그룹명', () => {
  test('테스트 설명', async ({ page }) => {
    // 페이지 이동
    await page.goto('/');

    // 요소 확인
    await expect(page.locator('h1')).toContainText('제목');

    // 클릭 액션
    await page.click('button');

    // URL 확인
    await expect(page).toHaveURL('/expected-url');
  });
});
```

### Locator 찾는 방법

```typescript
// 텍스트로 찾기
page.locator('text=Pinned')
page.locator('button:has-text("클릭")')

// CSS 셀렉터
page.locator('h1')
page.locator('.class-name')
page.locator('#id')

// 역할로 찾기
page.getByRole('button', { name: '제출' })
page.getByRole('heading', { level: 1 })

// 텍스트로 찾기 (권장)
page.getByText('정확한 텍스트')
```

## 🎯 다음 단계

1. **API 연동 후 테스트 추가**
   - CRUD 동작 테스트
   - 데이터 검증

2. **인증 테스트**
   - 로그인/로그아웃
   - 권한 확인

3. **에러 핸들링 테스트**
   - 네트워크 오류 시나리오
   - 404 페이지

4. **CI/CD 통합**
   - GitHub Actions
   - PR마다 자동 실행

## 📚 참고 자료

- [Playwright 공식 문서](https://playwright.dev)
- [Best Practices](https://playwright.dev/docs/best-practices)
- [Debugging Guide](https://playwright.dev/docs/debug)
