# PARA 모바일 앱

PARA 방법론을 기반으로 한 생산성 관리 모바일 앱입니다.

## 기능

### 5개 메인 화면

1. **🏠 Home** - 대시보드
   - 전체 현황 메트릭 카드
   - 오늘의 포커스 항목
   - 최근 활동 타임라인

2. **📋 Projects** - 프로젝트 관리
   - 단기 목표 프로젝트
   - 우선순위별 색상 구분
   - 진행률 및 마감일 표시
   - 필터링 및 정렬

3. **🎯 Areas** - 영역 관리
   - 지속적 관리 영역
   - 그리드 레이아웃
   - 습관 트래커
   - 진척도 시각화

4. **📚 Resources** - 자료 보관함
   - 참고 자료 라이브러리
   - 태그 기반 검색
   - Pinterest 스타일 그리드
   - 타입별 필터링

5. **📦 Archives** - 보관함
   - 완료된 항목 보관
   - 타임라인 뷰
   - 월별 통계
   - 회고 기능

## 기술 스택

- **Framework**: Flutter
- **State Management**: Riverpod
- **Code Generation**: Freezed, JSON Serializable
- **Architecture**: Clean Architecture
- **Design**: Material 3

## 프로젝트 구조

```
lib/
├── src/
│   ├── models/          # 데이터 모델 (Project, Area, Resource, Archive)
│   ├── theme/           # 디자인 시스템 (색상, 테마, 텍스트 스타일)
│   ├── widgets/         # 공통 위젯 (카드, 프로그레스바 등)
│   ├── features/        # 화면별 기능
│   │   ├── home/
│   │   ├── projects/
│   │   ├── areas/
│   │   ├── resources/
│   │   └── archives/
│   └── app.dart         # 메인 앱 & 네비게이션
└── main.dart
```

## 시작하기

### 1. 의존성 설치

```bash
flutter pub get
```

### 2. 코드 생성

```bash
flutter pub run build_runner build --delete-conflicting-outputs
```

### 3. 앱 실행

```bash
flutter run
```

## 디자인 시스템

### 색상 팔레트

- **Primary**: #4F46E5 (Indigo)
- **Projects**: #EF4444 (Red) - 긴급함, 마감일
- **Areas**: #10B981 (Green) - 지속성, 성장
- **Resources**: #F59E0B (Amber) - 지식, 참조
- **Archives**: #6B7280 (Gray) - 완료, 보관

### 타이포그래피

- **Title**: 22px, Bold
- **Heading**: 18px, SemiBold
- **Body**: 15px, Regular
- **Caption**: 13px, Regular

## 개발 가이드

### 새로운 화면 추가

1. `lib/src/features/` 아래에 새 폴더 생성
2. `presentation/` 폴더에 화면 위젯 작성
3. `lib/src/app.dart`의 네비게이션에 추가

### 새로운 모델 추가

1. `lib/src/models/` 아래에 모델 파일 생성
2. Freezed 어노테이션 사용
3. 코드 생성 실행:
   ```bash
   flutter pub run build_runner build --delete-conflicting-outputs
   ```

### 테마 커스터마이징

- `lib/src/theme/app_colors.dart` - 색상 변경
- `lib/src/theme/app_text_styles.dart` - 텍스트 스타일 변경
- `lib/src/theme/app_theme.dart` - 전체 테마 설정

## 라이선스

MIT
