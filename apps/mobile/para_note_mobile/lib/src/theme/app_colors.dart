import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const primary = Color(0xFF4F46E5); // Indigo

  // Feature Colors
  static const projectsColor = Color(0xFFEF4444); // Red
  static const areasColor = Color(0xFF10B981); // Green
  static const resourcesColor = Color(0xFFF59E0B); // Amber
  static const archivesColor = Color(0xFF6B7280); // Gray

  // Background Colors
  static const background = Color(0xFFF9FAFB); // Light Gray
  static const cardBackground = Colors.white;

  // Text Colors
  static const textPrimary = Color(0xFF111827);
  static const textSecondary = Color(0xFF6B7280);
  static const textTertiary = Color(0xFF9CA3AF);

  // Priority Colors
  static const priorityHigh = Color(0xFFEF4444);
  static const priorityMedium = Color(0xFFF59E0B);
  static const priorityLow = Color(0xFF10B981);

  // Priority Background Colors
  static const priorityHighBg = Color(0xFFFEE2E2);
  static const priorityMediumBg = Color(0xFFFEF3C7);
  static const priorityLowBg = Color(0xFFD1FAE5);

  // Border Colors
  static const border = Color(0xFFE5E7EB);
  static const divider = Color(0xFFE5E7EB);

  // Status Colors
  static const success = Color(0xFF10B981);
  static const warning = Color(0xFFF59E0B);
  static const error = Color(0xFFEF4444);
  static const info = Color(0xFF3B82F6);
}
